/* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * 
 * 
 *
 *
 * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

<% for(var setName in data) { const set = data[setName].js %><% for(var a in set) { const field = set[a] %>
import _<%= setName %><%= field.key %> from './<%= field.file %>'<% } %><% } %>

export default {
<% for(var setName in data) { const set = data[setName].js %> '<%= setName %>': {
    <% for(var a in set) { const field = set[a] %>'<%= field.key %>': _<%= setName %><%= field.key %>,
    <% } %>
  }<% } %>
}


